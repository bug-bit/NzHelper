<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- 暴露崩溃日志目录，供分享时通过 FileProvider 生成 content:// URI -->
    <files-path
        name="crash_logs"
        path="crash_logs/" />
    <!-- 成就分享图片（缓存目录，系统随时回收） -->
    <cache-path
        name="shared_images"
        path="shared_images/" />
</paths>
